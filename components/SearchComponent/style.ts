import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  searchForm: {
    padding: '2px 4px',
    display: 'flex',
    alignItems: 'center',
    width: '631px',
    height: '52px',
    background: '#5A5C66',
    margin: '15px 0',
  },
});
